/** @type {import('next').NextConfig} */
const nextConfig = {
  //   distDir: "build",
  // output: "export",
  images: {
    domains: ['lh3.googleusercontent.com'],
  },

};

export default nextConfig;
